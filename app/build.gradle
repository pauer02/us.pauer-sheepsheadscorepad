apply plugin: 'com.android.application'

android {

    signingConfigs {
        bigfoot1 {
            keyAlias 'bigfoot'
            keyPassword 'bigfoot1'
            storeFile file('/home/chris/android-studio/bin/bigfoot')
            storePassword 'bigfoot1'
        }
    }
    compileSdkVersion 17
    buildToolsVersion '27.0.3'
    defaultConfig {
        applicationId "us.pauer.android.sheepsheadscorepad"
        minSdkVersion 15
        targetSdkVersion 15
        signingConfig signingConfigs.bigfoot1
        versionCode 1
        versionName "1.1"
    }


    buildTypes {
        release {
            minifyEnabled false
            proguardFiles getDefaultProguardFile('proguard-android.txt'), 'proguard-rules.txt'
        }

    }
    compileOptions {
        sourceCompatibility JavaVersion.VERSION_1_7
        targetCompatibility JavaVersion.VERSION_1_7
    }
    productFlavors {
    }
}

dependencies {
    compile 'com.android.support:support-v4:18.0.0'
}
